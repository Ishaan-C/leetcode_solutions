public class IndexOfFirstOccurrence{
    public int strStr(String haystack, String needle) {

        return haystack.indexOf(needle);  //Just use the built-in function. If needle is not found in haystack, indexOf returns -1 on its own.
    
    /*RUNTIME- 0ms. Beats 100% */
}

}